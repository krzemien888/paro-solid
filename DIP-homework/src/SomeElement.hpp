#include <string>

using namespace std;

class SomeElement
{
public:
	int x,y;
	string name;

	SomeElement(int inputX, int inputY, string inputName):x(inputX), y(inputY), name(inputName){}
};
